apiVersion: v1
kind: Service
metadata:
  name: prac9p-mongo-svc
spec:
  selector:
    app: prac9p-mongo
  type: NodePort
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 32001

  